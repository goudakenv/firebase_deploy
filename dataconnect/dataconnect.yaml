specVersion: "v1"
serviceId: "firebasedeploy"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebasedeploy-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
